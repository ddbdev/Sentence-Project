spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ddbdev/Team-A-ASW-850
          username: ${GITUSER}
          password: ${AUTHTOKEN}
        default-label: main
        enabled: true